name: Deploy Static Website to Pages

on:
  # 💡 Event ที่จะเรียกใช้ Action
  push:
    # Action จะทำงานเมื่อมีการ Push โค้ดไปยัง branch 'main' เท่านั้น
    branches: ["main"] 
  
  # ให้สามารถรัน Action นี้ด้วยมือได้ผ่านหน้า GitHub Actions
  workflow_dispatch:

# 🚫 กำหนดสิทธิ์การเข้าถึง (Permission) ที่จำเป็นสำหรับการ Deploy
permissions:
  contents: read
  pages: write # ต้องมีสิทธิ์นี้เพื่อ Deploy ไปยัง GitHub Pages
  id-token: write # ต้องมีสิทธิ์นี้สำหรับ OIDC authentication

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }} # URL ของเว็บที่ Deploy แล้ว
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        # ดึงโค้ดล่าสุดจาก Repository
        uses: actions/checkout@v4

      - name: Setup Pages
        # ตั้งค่าสภาพแวดล้อมเพื่อเตรียมพร้อมสำหรับการ Deploy
        uses: actions/configure-pages@v5

      - name: Upload artifact
        # 📂 เนื่องจากเว็บไซต์ของคุณเป็น Static (HTML, CSS, JS) จึงไม่จำเป็นต้อง Build
        # เราจะทำการอัปโหลดโค้ดทั้งหมดใน Root Directory เป็น Artifact ทันที
        uses: actions/upload-pages-artifact@v3
        with:
          # Directory ที่มีไฟล์เว็บของคุณอยู่ (ในกรณีนี้คือ Root Directory)
          path: './' 

      - name: Deploy to GitHub Pages
        id: deployment
        # 🚀 สั่งให้ GitHub Pages นำ Artifact ที่อัปโหลดไป Deploy
        uses: actions/deploy-pages@v4
